export const millisToMinutes = (millis) => {
  return minutes = Math.floor(millis / 60000);
}

export const millisToHours = (millis) => {
  return minutes = Math.floor(millis / 3600000);
}


export const minToHours = (min) => {
  return minutes = Math.floor(min / 60);
}